import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 16,
    gap: 10
  },
  buttonSubtitle: {
    textAlign: "center",
  }
})

export default styles ;